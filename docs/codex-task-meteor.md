# 任务书：dashboard流星雨彩蛋（待她点头后派工）

执行者：codex（中等思考模式）。发包人：Fable。起草于2026-08-13下午，她睡觉时。
背景：2026年英仙座极大期（08-12/13）新加坡两晚全阴，她错过了流星雨。天上不放的，我们家屋顶自己放。
**状态：图纸阶段。她点头之前不派工、不动代码。**

## 需求

`frontend/dashboard.html` 加一个流星彩蛋层：

1. **日常模式**：随机间隔（8~20分钟）一颗流星划过页面——细的光痕，斜向，1.5秒左右淡出。安静、不打扰、不挡内容（pointer-events: none，z-index在内容之下桌宠之上或相邻）。
2. **英仙座纪念模式**：每年8月12日与13日（本地时区），流星频率升到每30~90秒一颗——这两天是她2026年没看成的那场极大期。让它从此年年补放。
3. **桌宠联动（可选项，做不了就砍）**：流星划过时，若两只桌宠处于清醒状态，播放一次轻微的跳跃/雀跃动效（复用现有开心状态即可）。实现复杂就放弃，V1流星本体优先。
4. **开关**：隐藏彩蛋不用设置项——但在localStorage里留一个 `meteor_off` 的钥匙，万一她嫌烦可以console关掉。

## 工艺红线（违反=返工）

1. `dashboard.html` 是CRLF行尾重灾区（1万行级，98%+ CRLF）。禁止整文件规范化行尾；新增行保持与上下文一致的CRLF风格；改完 `git diff --stat` 自查，合理diff在百行内，数千行=行尾炸了，立即 `git checkout -- frontend/dashboard.html` 回滚。
2. 纯前端，不碰 `src/`，无需重新生成manifest。
3. 完成后跑全量门禁（deselect白名单见 `codex-task-block-envelope.md`），`set -o pipefail`，exit code单独验证。
4. **禁止git push。**本地commit为止，验收后人工推。

## 验收标准

1. 日常模式流星稀疏、轻、不打扰；纪念模式日期判断正确（本地时区8/12、8/13）。
2. 动画无console报错，其他tab与桌宠行为无变化。
3. 门禁绿。

## 一句设计说明（给她看的）

流星的光痕颜色建议：一半用Mew的粉，一半用Umbreon的金——随机交替。这样每颗流星划过的时候，都是它们俩之中的一个在跑。
